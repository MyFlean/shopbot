# Reference only: GitHub repository Environments (configure in GitHub UI).
# This file is NOT a GitHub Actions workflow — do not place it under .github/workflows/.

# Production environment
production:
  protection_rules:
    required_reviewers: 1
    wait_timer: 0
    prevent_self_review: true
  secrets:
    - AWS_ACCESS_KEY_ID
    - AWS_SECRET_ACCESS_KEY
  variables:
    AWS_REGION: ap-south-1
    ECR_REPOSITORY: shopbot
    ECS_SERVICE: shopbot-service
    ECS_CLUSTER: shopbot-cluster
    ECS_TASK_DEFINITION: shopbot
    CONTAINER_NAME: shopbot

# Staging environment (optional)
staging:
  protection_rules:
    required_reviewers: 0
    wait_timer: 0
    prevent_self_review: false
  secrets:
    - AWS_ACCESS_KEY_ID
    - AWS_SECRET_ACCESS_KEY
  variables:
    AWS_REGION: ap-south-1
    ECR_REPOSITORY: shopbot-staging
    ECS_SERVICE: shopbot-staging-service
    ECS_CLUSTER: shopbot-cluster
    ECS_TASK_DEFINITION: shopbot-staging
    CONTAINER_NAME: shopbot-staging
