"""
Any additional engine-selection logic beyond what
shopping_bot/routes/unified_search.py's _search_engine() already implements.

Placeholder — no known gap yet; existing SEARCH_ENGINE auto/v1/v2 routing
already covers the query-driven search endpoint correctly.
"""
