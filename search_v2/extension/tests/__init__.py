"""
Regression tests for every extension/ module, run after each migration
phase per the "implement one capability at a time, regression after each"
policy. Mirrors the structure of the other extension/ subpackages.
"""
